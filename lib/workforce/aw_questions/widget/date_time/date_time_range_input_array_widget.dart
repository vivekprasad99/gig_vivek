import 'package:flutter/material.dart';

class DateTimeRangeInputArrayWidget extends StatelessWidget {

  const DateTimeRangeInputArrayWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
