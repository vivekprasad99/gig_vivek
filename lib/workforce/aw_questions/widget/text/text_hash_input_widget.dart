import 'package:awign/workforce/aw_questions/data/model/question.dart';
import 'package:flutter/cupertino.dart';

class TextHashInputWidget extends StatelessWidget {
  Question? question;

  TextHashInputWidget(this.question, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
